[project]
name = "dify"
version = "0.1.0"
description = "Dify - 添加代理服务器依赖"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.2",
    "flask-cors>=5.0.0",
    "requests>=2.31.0",
]
