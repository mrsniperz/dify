# 内容解码失败和404错误修复指南

## 问题分析

### 问题1: ERR_CONTENT_DECODING_FAILED
**原因**: 目标服务器返回的gzip压缩响应未正确解压

### 问题2: 静态资源404
**原因**: 字体文件等静态资源的路径解析问题

## ✅ 已修复

1. **添加gzip解压处理**
   ```python
   if response.headers.get('Content-Encoding') == 'gzip':
       content = gzip.decompress(response.content)
   ```

2. **改进路径解析**
   - 正确处理静态资源路径
   - 避免双斜杠问题

3. **过滤问题响应头**
   - 移除transfer-encoding
   - 移除content-encoding
   - 添加no-cache控制

4. **增强CORS支持**
   - 添加CORS预检请求处理
   - 添加完整的CORS头部

## 🚀 测试步骤

### 步骤1: 重启代理服务
```bash
cd /mnt/e/Projects/dify
./start-proxy.sh
```

### 步骤2: 检查日志
启动后应该看到：
```
[session_id] 代理请求: GET /proxy/.../ -> https://fy.anyremote.cn:8334/
[session_id] 代理响应: 200 (15234 bytes)
```

### 步骤3: 使用简单测试页面
在浏览器中打开：
```
file:///mnt/e/Projects/dify/simple-test.html
```

**预期结果**：
- ✅ 页面正常显示
- ✅ 没有 ERR_CONTENT_DECODING_FAILED 错误
- ✅ 没有静态资源404错误
- ✅ 可以看到Dify登录界面

## 🔍 调试信息

### 检查健康状态
```bash
curl http://localhost:5005/health
```

应该看到：
```json
{
  "active_sessions": 1,
  "status": "healthy",
  ...
}
```

### 直接测试代理
```bash
# 生成新session
SESSION=$(python3 -c "import uuid; print(str(uuid.uuid4())[:12])")
echo "Session: $SESSION"

# 测试根路径
curl -v "http://localhost:5005/proxy/$SESSION/" 2>&1 | head -20

# 测试静态资源
curl -v "http://localhost:5005/proxy/$_/_next/static/media/test.woff2" 2>&1 | head -20
```

### 查看浏览器网络面板
1. 打开开发者工具（F12）
2. 切换到Network面板
3. 刷新测试页面
4. 检查请求：
   - 状态码应为200（不是404或解码错误）
   - Content-Type应正确
   - 不应出现Content-Encoding: gzip

## 🛠️ 故障排除

### 问题1: 仍然是 ERR_CONTENT_DECODING_FAILED

**检查代理日志**：
```
[session_id] 代理响应: 200 (15234 bytes)
```

如果响应大小为0，可能是压缩问题。

**解决方案**：
检查`simple_proxy.py`第256-263行的gzip解压代码是否执行。

### 问题2: 静态资源仍然是404

**检查请求路径**：
在日志中查看：
```
代理请求: GET /proxy/session/_next/static/media/test.woff2 -> https://fy.anyremote.cn:8334/_next/static/media/test.woff2
```

如果路径错误，检查路径构建逻辑（第220-233行）。

**测试静态资源**：
```bash
curl -v "http://localhost:5005/proxy/session123/_next/static/media/test.woff2"
```

应该返回：
- 状态码200（不是404）
- 有内容返回（不是空）

### 问题3: 页面加载但显示空白

**检查控制台错误**：
1. 打开浏览器开发者工具
2. 查看Console面板
3. 检查是否有JavaScript错误

**检查DOM加载**：
```javascript
// 在浏览器控制台中执行
document.body.innerHTML.length
```

应该返回大于0的值。

## 🔧 高级调试

### 启用详细日志
在`simple_proxy.py`中添加：
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### 检查原始响应
修改代理代码，添加：
```python
print(f"原始响应头: {dict(response.headers)}")
print(f"内容编码: {response.headers.get('Content-Encoding')}")
print(f"内容类型: {response.headers.get('Content-Type')}")
```

### 禁用压缩请求
修改请求头部：
```python
headers['Accept-Encoding'] = 'identity'
```

## 📊 预期性能

### 成功指标
- ✅ 健康检查: 200 OK
- ✅ 代理响应: 200 OK
- ✅ 响应大小 > 0
- ✅ 页面加载 < 5秒
- ✅ 无404错误
- ✅ 无解码错误

### 失败指标
- ❌ 健康检查: 500/错误
- ❌ 代理响应: 404/500
- ❌ 响应大小 = 0
- ❌ 页面空白
- ❌ 控制台错误

## 💡 最佳实践

1. **每次测试前重启代理**
   ```bash
   ./start-proxy.sh
   ```

2. **使用新的Session ID**
   - 避免缓存问题
   - 确保会话隔离

3. **检查实时日志**
   - 保持代理启动窗口可见
   - 观察所有请求和响应

4. **分步测试**
   - 先测试健康检查
   - 再测试代理根路径
   - 最后测试iframe加载

## 📞 获取帮助

如果仍然失败，请提供：
1. 代理服务器完整启动日志
2. `curl http://localhost:5005/health` 输出
3. 浏览器Network面板截图
4. 代理窗口中的请求日志
5. 使用的Session ID

## 更新日志

- **v1.4** - 修复内容解码失败问题
- **v1.3** - 修复静态资源404问题
- **v1.2** - 添加CORS支持
- **v1.1** - 修复路由问题
- **v1.0** - 初始版本

---

**注意**: 这些修复专门针对 Next.js 和 React 应用的代理场景
