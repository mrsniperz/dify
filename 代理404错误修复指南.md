# 代理服务器 404 错误修复指南

## 问题描述

使用代理访问时，iframe 显示 "Not Found" 并返回 404 错误。

## 修复方案 ✅

已创建简化版代理服务器 `simple_proxy.py`，专门解决了路由和404问题。

## 使用步骤

### 1. 启动新的代理服务

```bash
cd /mnt/e/Projects/dify

# 使用新的启动脚本
./start-proxy.sh
```

**预期输出**：
```
==========================================
Dify iframe 代理服务启动器 (uv版本)
==========================================
✓ 找到 uv 版本: uv 0.x.x
✓ 使用 Python 版本: 3.13.x
✓ 依赖检查通过
...
启动中...

==========================================
Dify iframe 简化代理服务器
==========================================
目标服务器: https://fy.anyremote.cn:8334
代理基础路径: /proxy/<session_id>/

使用方法:
1. 在浏览器中打开测试页面
2. 选择'通过代理访问'
3. 输入 URL: http://localhost:5005/proxy/<session_id>/

健康检查: http://localhost:5005/health
==========================================

* Running on http://127.0.0.1:5005
* Running on http://[你的IP]:5005
```

### 2. 测试代理服务

在新的终端窗口中运行：
```bash
cd /mnt/e/Projects/dify
./test-proxy.sh 5005
```

**预期结果**：
```
测试: 健康检查
✅ 成功
测试: 主页
✅ 成功
```

### 3. 使用测试页面

1. 在浏览器中打开：`file:///mnt/e/Projects/dify/test-iframe-proxy.html`

2. 在测试页面中：
   - 目标URL: `https://fy.anyremote.cn:8334`
   - 选择: **"通过代理访问"**
   - 代理URL会自动填写: `http://localhost:5005/proxy/`

3. 点击 **"开始测试"**

4. 在 iframe 内进行登录

## 重要修改说明

### 新代理服务器改进

1. **简化的路由**
   ```python
   @app.route('/proxy/<session_id>', defaults={'path': ''})
   @app.route('/proxy/<session_id>/<path:path>')
   ```
   - 自动处理根路径和子路径
   - 支持 `/proxy/session_id/` 访问根URL

2. **智能URL构建**
   - 自动从路径中提取目标URL
   - 正确处理查询参数

3. **增强的HTML重写**
   - 更可靠的正则表达式替换
   - 添加自动检测脚本
   - Base标签确保相对路径正确

4. **详细日志**
   ```python
   print(f"[{session_id}] 代理请求: {method} {path} -> {target_url}")
   print(f"[{session_id}] 代理响应: {status} ({bytes} bytes)")
   ```
   - 可以查看代理日志分析问题

## 故障排除

### 问题 1: 仍然是 404

**解决方案**：
1. 检查代理服务器是否正常运行
   ```bash
   curl http://localhost:5005/health
   ```

2. 检查代理日志输出，查看请求是否被接收

3. 确认测试页面使用的是代理URL
   - 例如：`http://localhost:5005/proxy/abc123/`

### 问题 2: 代理无法连接到目标服务器

**检查网络**：
```bash
curl -k https://fy.anyremote.cn:8334
```

如果失败，可能是：
- 网络连接问题
- 防火墙阻止
- SSL证书问题

### 问题 3: 测试页面代理URL错误

在测试页面中，确保代理URL格式为：
```
http://localhost:5005/proxy/<随机ID>/
```

**错误的URL**：
- ❌ `http://localhost:5005/`
- ❌ `http://localhost:5005/proxy/`
- ❌ `http://localhost:5005/proxy/abc123`

**正确的URL**：
- ✅ `http://localhost:5005/proxy/abc123/`
- ✅ `http://localhost:5005/proxy/abc123/index.html`
- ✅ `http://localhost:5005/proxy/abc123/login`

### 问题 4: Session ID 不匹配

代理服务器会从请求URL中提取session ID。如果请求中没有session ID，代理会自动生成一个。

**测试session ID**：
```bash
# 查看活跃会话
curl http://localhost:5005/health | jq
```

## 最佳实践

1. **使用新的简化代理**
   ```bash
   ./start-proxy.sh
   ```

2. **查看代理日志**
   - 启动代理后会显示详细请求日志
   - 注意 `[session_id]` 标记

3. **健康检查**
   - 定期访问 `http://localhost:5005/health`
   - 确认服务状态

4. **清理会话**
   - 代理每1小时自动清理过期会话
   - 如需手动清理，重启代理服务

## 高级用法

### 手动测试代理
```bash
# 生成会话ID
SESSION_ID=$(python3 -c "import uuid; print(str(uuid.uuid4())[:12])")
echo "会话ID: $SESSION_ID"

# 测试代理访问
curl -v "http://localhost:5005/proxy/$SESSION_ID/"
```

### 使用不同端口
```bash
# 启动在端口5007
./start-proxy.sh 5007

# 测试
./test-proxy.sh 5007
```

## 技术细节

### 路由解析
- URL: `/proxy/session123/`
  - session_id: `session123`
  - path: `` (空)
  - 目标URL: `https://fy.anyremote.cn:8334/`

- URL: `/proxy/session123/login`
  - session_id: `session123`
  - path: `login`
  - 目标URL: `https://fy.anyremote.cn:8334/login`

### 内容重写
1. 将 `https://fy.anyremote.cn:8334` 替换为 `/proxy/session_id`
2. 添加 `<base href="https://fy.anyremote.cn:8334/">`
3. 注入 JavaScript 检测登录状态

## 联系支持

如果仍有问题，请提供：
1. 代理服务器完整日志
2. `curl http://localhost:5005/health` 的输出
3. 浏览器开发者工具的 Network 面板截图
4. 测试页面使用的完整URL

## 更新日志

- **v1.3** - 创建简化代理服务器，修复404错误
- **v1.2** - 修复URL重写问题
- **v1.1** - 添加自动登录检测
- **v1.0** - 初始版本

---

**重要提醒**: 这个简化代理服务器是专门为测试和开发设计的。生产环境请使用更安全的认证机制。
